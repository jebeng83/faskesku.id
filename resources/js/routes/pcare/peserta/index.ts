import nik from './nik'
import noka from './noka'
import nokartu from './nokartu'

const peserta = {
    nik: Object.assign(nik, nik),
    noka: Object.assign(noka, noka),
    nokartu: Object.assign(nokartu, nokartu),
}

export default peserta